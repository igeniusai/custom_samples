dns:
  postfix: "" # TODO: add postfix for QA environment, e.g. "qa.crystal.io"
kubernetes: 
  context: crystal-qa-azure-us
helm:
  chart: ./kubernetes/charts/services
  namespace: test
  # values_files: 
  #   - ./kubernetes/values.test.yaml
registries: 
  - eu.gcr.io/igenius-registry
containers:
  platform: "linux/amd64"
services:
  path: ./services/
