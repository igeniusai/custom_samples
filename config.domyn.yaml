dns:
  postfix: "local"
kubernetes: 
  context: docker-desktop
helm:
  chart: ./kubernetes/charts/services
  namespace: test
  # values_files: 
  #   - ./kubernetes/values.test.yaml
registries: 
  - eu.gcr.io/igenius-registry
containers:
  platform: "linux/amd64"
services:
  path: ./services/
  build_context:
    langgraph_agent_controller: ./services/
