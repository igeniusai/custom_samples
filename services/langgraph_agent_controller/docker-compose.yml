services:
  langgraph-agent-controller:
    build:
      context: ..
      dockerfile: langgraph_agent_controller/Dockerfile
    ports:
      - "9081:8080"
    restart: unless-stopped
    environment:
      - AGENT_WORKDIR=/srv/agent
