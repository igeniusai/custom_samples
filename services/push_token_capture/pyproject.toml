[project]
name = "push-token-capture"
version = "0.1.0"
description = "Captures the push token agents-service hands to a REST tool, so the QA parts suites can push external parts."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "pydantic>=2.13.4",
    "uvicorn[standard]>=0.46.0",
]
