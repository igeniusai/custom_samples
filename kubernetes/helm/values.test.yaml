services:
  - name: example1
    image:
      registry: "reg1"
      repository: test1
      tag: "latest"
      pullPolicy: Never
    type: LoadBalancer
    tls:
      enabled: false
    ports:
      - port: 8080
        targetPort: 80
        name: http
        protocol: TCP
