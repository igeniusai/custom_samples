[project]
name = "domyn"
version = "0.1.0"
dependencies = ["typer", "PyYAML"]

[project.scripts]
domyn = "domyn.main:app"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
